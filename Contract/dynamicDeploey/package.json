{
  "name": "rewardPoints",
  "version": "0.0.2",
  "description": "",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "solc": "concurrently \"solcjs --optimize --bin -o ../build/ ../Contracts.sol\" \"solcjs --optimize --abi -o ../build/ ../Contracts.sol\"",
    "release": "node All.js release",
    "deploey": "node deploey.js",
    "debug": "node All.js debug"
  },
  "author": "RubyCheng",
  "license": "MIT",
  "dependencies": {
    "web3": "^0.18.2"
  },
  "devDependencies": {
    "concurrently": "^3.2.0",
    "solc": "0.4.8"
  }
}
